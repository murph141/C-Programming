
   Usage: pa01 <filename>

   Input files are in the "inputs" directory. Thus you can
   try something like:

      aturing > ./pa01 inputs/input0

   and you should get something like:

      sum = -3772.
      The array has 54 negative elements.
      The elements are not increasing.

